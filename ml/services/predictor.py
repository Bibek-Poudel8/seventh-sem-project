def forecast(transactions: list, months_ahead: int = 1) -> dict:
    """
    Placeholder — implement expense prediction model here later.
    transactions: list of { amount, category, date }
    returns: { predictions: [{ category, predicted_amount, month }] }
    """
    raise NotImplementedError('Expense predictor not built yet')